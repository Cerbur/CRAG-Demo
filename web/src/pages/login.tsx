import type { JSX } from 'react';
import { PagePlaceholder } from './page-placeholder';

export function LoginPage(): JSX.Element {
  return <PagePlaceholder title="登录" description="登录表单将在后续任务中实现。" />;
}
