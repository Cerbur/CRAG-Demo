import type { JSX } from 'react';
import { PagePlaceholder } from './page-placeholder';

export function RegisterPage(): JSX.Element {
  return <PagePlaceholder title="注册" description="注册表单将在后续任务中实现。" />;
}
