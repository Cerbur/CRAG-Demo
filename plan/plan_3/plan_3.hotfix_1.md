# Plan_3.hotfix_1 — 代码风格约束文档抽取

> 创建时间：2026-06-12
> 归属：plan_3 文档资产建设后的协作规范整理

---

## 背景

准备继续推进后续代码任务前，需要将代码风格约束从 Agent 入口文档中抽取到独立 Markdown，避免 `AGENTS.md` 与 `CLAUDE.md` 重复维护同一批规则。

本次同时补充两条明确约束：

1. Java 代码不得出现 `import *`。
2. Spring 依赖注入优先使用 `@Autowired` 字段注入，不优先使用构造器注入。

---

## 范围

1. 新增 `constraints/code-style.md`，集中维护 Java 代码风格、注释和设计约束。
2. 更新 `AGENTS.md`，增加代码风格约束路由。
3. 更新 `CLAUDE.md`，增加代码风格约束路由。
4. 更新 `plan/plan_main.md`，将代码规范章节收敛为指向独立文档的入口。

---

## 进度追踪

| 编号 | 任务 | 状态 | 提交 | 完成时间 |
| --- | --- | --- | --- | --- |
| 3.hotfix_1.1 | 新增代码风格约束文档 | ✅ 完成 | — | 2026-06-12 |
| 3.hotfix_1.2 | AGENTS.md 增加约束路由 | ✅ 完成 | — | 2026-06-12 |
| 3.hotfix_1.3 | CLAUDE.md 增加约束路由 | ✅ 完成 | — | 2026-06-12 |
| 3.hotfix_1.4 | plan_main 代码规范入口收敛 | ✅ 完成 | — | 2026-06-12 |
| 3.hotfix_1.5 | 检查文档路由与新增约束 | ✅ 完成 | — | 2026-06-12 |

> 状态图例：⏳ 待开始 / 🔄 进行中 / ✅ 完成 / ❌ 阻塞

整体进度：**5 / 5（100%）**

---

## 验收标准

- `constraints/code-style.md` 存在，并包含 `import *` 禁止规则。
- `constraints/code-style.md` 存在，并包含 `@Autowired` 优先规则。
- `AGENTS.md` 与 `CLAUDE.md` 不直接展开代码风格细则，只路由到 `constraints/code-style.md`。
- `plan_main` 的代码规范章节不再作为细则主维护点。

---

## 变更记录

| 日期 | 变更 |
|------|------|
| 2026-06-12 | 创建并完成代码风格约束文档抽取 hotfix |
